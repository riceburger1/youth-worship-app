# V35 날씨 화면 최하단 이동 수정

- 학생 화면에서 날씨 섹션을 `<main>`의 마지막 섹션으로 이동했습니다.
- 이전 HTML이 브라우저/PWA 캐시에 남아 있어도 `app.js`가 실행되면 날씨 섹션을 학생 화면 최하단으로 다시 배치합니다.
- 관리자 화면에서는 기존처럼 날씨 섹션을 숨깁니다.
- `내 위치` 버튼의 작은 모바일 크기와 `예배 가는 길 날씨` 한 줄 표시 설정은 유지합니다.
- Supabase SQL 변경 없음.
